[project]
name = "columns-ec2"
version = "0.1.0"
description = "Reinforced concrete column analysis and design tool for EC2 workflows"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["columns_ec2*"]
